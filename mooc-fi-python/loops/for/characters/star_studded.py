# print each character in a string on a separate line
# after each line print a star
# use a for lop (definite iteration)

word = input('Enter a word: ')

for character in word :
    print(character)
    print('*')